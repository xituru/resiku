query Track($awb: String!, $courier: Courier) {
  Track(awb: $awb, courier: $courier) {
    status
    summary {
      awb
      courier
      service
      date
      desc
      amount
      weight
    }
    history {
      date
      desc
      location
    }
  }
}
